import 'package:flutter/material.dart';

final appTheme = ThemeData(
  primarySwatch: Colors.amber,
  textTheme: TextTheme(
    display4: TextStyle(
      fontSize: 24,
      color: Colors.black,
    )
  )
);